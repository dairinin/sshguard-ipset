# Configuration for netfilter-persistent
# Plugins may extend this file or have their own

FLUSH_ON_STOP=0

# Only listed ipsets are saved/restored
# All sets if not set
# Sets with names ending in '*' are saved as definitions only
# Single '*' saves all sets as definitions
MANAGED_SETS=
